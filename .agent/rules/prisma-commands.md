---
trigger: always_on
---

when applity migration in local setup always use 'npx prisma migrate dev' and when working on production serever through ssh from terminal then always use 'npx prisma migrate deploy' for applying migration
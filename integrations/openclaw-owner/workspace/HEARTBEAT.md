# Heartbeat

No autonomous recurring project work is authorized by default.

Do not create schedules, reminders, background jobs, outbound notifications, or
self-initiated tasks. A future heartbeat requires a separately reviewed
read-only capability, explicit owner authorization, proven notification routing,
and a documented stop condition.

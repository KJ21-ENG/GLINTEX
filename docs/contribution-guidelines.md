# Contribution Guidelines

No dedicated `CONTRIBUTING.md` file was found.

Current observed conventions:
- Use workspace scripts from root `package.json` for frontend/backend.
- Maintain compatibility with process-permission model (`issue.*`, `receive.*`, etc.).
- Keep Prisma schema/migrations synchronized with route behavior changes.
- Validate major inventory flows manually (inbound, issue, receive, dispatch, reports).

